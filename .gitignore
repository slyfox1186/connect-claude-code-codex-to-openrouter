# ---------------------------------------------------------------------------
# Secrets. This repo is public, so these come first and stay broad.
# The OpenRouter key is read at runtime from ~/.config/openrouter/env and has
# never lived in the tree. These rules exist so it cannot start to.
# ---------------------------------------------------------------------------
env
.env
.env.*
*.env
env.local
secrets.json
*secret*
*credential*
*_token
*.token
.netrc
.npmrc
.pypirc
.git-credentials

# Keys and certificates
*.pem
*.key
*.crt
*.p12
*.pfx
*.keystore
*.jks
id_rsa*
id_dsa*
id_ecdsa*
id_ed25519*

# ---------------------------------------------------------------------------
# This bridge's own runtime state. Defaults live under ~/.local/state and
# ~/.cache, but ORASK_STATE_DIR / ORASK_CACHE_DIR / ORASK_CONFIG_DIR can point
# them anywhere, including here. The call log and saved threads hold the full
# text of every question asked, so they must never be published.
# ---------------------------------------------------------------------------
calls.jsonl
diagnostics.jsonl*
threads/
consultations/
.cache/
cache/
state/
config/config.json
config/*.local.json

# The packaged defaults ARE part of the project: never let a rule above or
# below swallow them.
!config/models.json
!.gitignore
!.gitattributes

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
sdist/
wheels/
*.egg-info/
*.egg
.installed.cfg
MANIFEST

# Virtual environments. The project normally runs from its own conda env
# (envs/openrouter-mcp under whatever conda root the machine has), but on a
# machine with no conda install.sh builds a project-local .venv instead, so one
# can legitimately exist here. It still never gets committed.
.venv/
venv/
ENV/
.conda/
conda-meta/

# The interpreter install.sh resolved on this machine. Machine-specific by
# definition: the launchers read it, git never sees it.
.orask-python

# Tooling caches
.pytest_cache/
.mypy_cache/
.dmypy.json
.ruff_cache/
.tox/
.nox/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.cover

# ---------------------------------------------------------------------------
# Agent and editor local state
# ---------------------------------------------------------------------------
.claude/settings.local.json
.claude/memory/
.mcp.local.json
.codex/
.vscode/
.idea/
*.iml
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ---------------------------------------------------------------------------
# Scratch and rejected work
# ---------------------------------------------------------------------------
*.log
*.bak
*.orig
*.rej
*.tmp
tmp/
scratch/
scratchpad/

# Local only: personal setup notes, never published.
JEFF_START_HERE.md

# Agent guidance for working ON this repo. Local by choice: it names this
# machine's conventions, and a clone elsewhere should not inherit them.
CLAUDE.md

# Raw source material for the cheat sheets in guides/. Local only: large,
# machine-specific, and not this project's to publish.
docs/
